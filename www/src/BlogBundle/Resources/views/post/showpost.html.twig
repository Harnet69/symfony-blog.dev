{% extends 'base.html.twig' %}

{% block body %}
         <h2>{{ post.title|upper }}</h2>
        <p>{{ post.description }}
        <p>{% if post.datetime %}{{ post.datetime|date('Y-m-d H:i') }}{% endif %}</p>
{% endblock %}